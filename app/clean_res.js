const fs = require("fs"); let lines = fs.readFileSync("index.html", "utf8").split(/\r?\n/); const idx = lines.findIndex(l => l.includes("Resolution Quality")); if (idx !== -1) { // 找到包含 Resolution Quality 的那一行，將其上方的 \r\n 徹底清掉\n lines[idx-1] = "                <div class=\"input-group\" style=\"margin-top: 24px;\">";\n lines[idx] = "                    <label>Resolution Quality</label>";\n fs.writeFileSync("index.html", lines.join("\n"), "utf8"); console.log("Cleaned line " + idx);\n} else { console.log("Not found"); }
